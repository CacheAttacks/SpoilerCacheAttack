{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build victim",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g", "victim.c", "-o", "victim"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
